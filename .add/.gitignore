# ADD engine transient artifacts — local working state, never committed.
# (Scaffolded by `add.py init`; edit freely — init never clobbers an existing copy.)
scope-snapshot.json
pre-archive-state.bak.json
snapshot_before.json

# ADD dogfood mirror: tooling/ is a regenerable copy of add-method/tooling/
# (the canonical engine source) — not committed, mirrors the docs/ rationale above.
tooling/

# ADD dogfood mirror: personas-teacher/ is a regenerable copy of add-method/personas-teacher/
# (the canonical vendored teacher snapshot add-persona reads) — OPTIONAL managed tree the
# installer materializes for consumer projects; this repo's own copy is opportunistic/local-
# only, same rationale as tooling/ above.
personas-teacher/