# TASK: Bless milestone-free fast tasks as a first-class low-ceremony lane

slug: standalone-fast-task · created: 2026-06-25 · stage: mvp
autonomy: auto
phase: done   <!-- fast lane: ground -> specify -> contract -> tests -> build -> verify -> observe -> done -->
fast: true   <!-- the fast lane: a small task, collapsed flow + minimal template. Omit --fast for full rigor. -->

> Fast lane — one small task, minimal sections, filled top-to-bottom. The trust floor still
> holds: a FROZEN §3 contract · ≥1 red test before build · a recorded §6 gate (security = HARD-STOP).
> The acceptance scenario collapses into §1 `Accept:`; OBSERVE is one optional line at the gate.

---

## 0 · GROUND — the real codebase

Touches (files · symbols):
- `add-method/tooling/add.py:cmd_new_task` (1013–1118) — create-time orphan note: the `else` branch (no owning milestone) at 1107–1114 prints the intake nudge. `fast` is a local (the `--fast` flag); the durable marker `state["tasks"][slug]["fast"]=True` is set at 1103.
- `add-method/tooling/add.py:cmd_check` (orphan warn) — 2634–2642: when a task's `milestone is None` it appends a WARN "is outside a milestone". `t.get("fast")` is readable per task here.
- engine mirrors (parity-locked): `.add/tooling/add.py` (dogfood) · `add-method/src/add_method/_bundled/tooling/add.py` (bundled — regenerated by `add-method/scripts/prepare_bundle.py`) · pin `add-method/tooling/engine_pin.py:ENGINE_MD5` (re-pinned on any engine edit; guarded by `test_shared_engine_pin.py` + `test_tree_parity.py`).
- guard to keep green: `add-method/tooling/test_v8_1_orphan_guard.py` — scenarios 1/3/5/6 assert the NON-fast orphan nudge/warn; my change must not touch the non-fast path.

Anchors the contract cites: `cmd_new_task` no-milestone branch · `cmd_check` orphan-warn branch · the per-task `fast` marker.

---

## 1 · SPECIFY — the rules

Feature: Blessed milestone-free fast lane (standalone fast task)
Must:
  - When `new-task <slug> --fast` runs with NO owning milestone, stdout AFFIRMS a deliberate standalone fast lane (no "size it via intake" nudge).
  - `check` must NOT warn a milestone-less task as "outside a milestone" when that task carries the `fast` marker (a blessed lane, not an orphan to size).
  - The NON-fast milestone-less path is UNCHANGED — `new-task <slug>` still nudges; `check` still warns (warn-never-block preserved → keeps the v8.1 orphan guard green).
Reject:
  - (none — warn-never-block; no new hard failure, no new error code)
Accept: Given a fresh project with no active milestone, When I run `new-task t --fast`, Then stdout affirms a standalone fast lane (no intake nudge) AND a following `check` emits a soft INFO line for `t` (NOT a WARN — `warned` not inflated) — while `new-task u` (no --fast) still nudges and `check` still WARNs `u`.
Assumptions: ⚠ RESOLVED — soft-info (human chose visible affirmation). Remaining risk: parity/pin drift across the 3 engine trees if mirror+re-pin is skipped; and the additive `infos` JSON key must not perturb existing key assertions.

---

## 3 · CONTRACT — freeze the shape

```
cmd_new_task — when the task has NO owning milestone:
  fast is True   -> "standalone fast task '<slug>' — milestone-free by design (low-ceremony lane);
                    attach later with `add.py set-milestone <slug> --milestone <id>` if it grows"
  fast is False  -> UNCHANGED: "note: '<slug>' is not attached to a milestone — size it via /add
                    (intake), or pass --milestone <id>"

cmd_check — for a task whose milestone is None:
  t.get("fast") truthy   -> SOFT INFO (new additive tier): "task '<slug>' — standalone fast lane
                            (milestone-free by design)". NOT counted in `warned`, NOT a `failed`.
                            Text: `INFO  ...`; --json: additive `infos` array + `informed` count.
  else                   -> UNCHANGED: WARN "task '<slug>' is outside a milestone — size it via
                            the /add intake flow (or attach with --milestone)"

Out of THIS task's scope (owned by loose-task-release): release attribution of done standalone
fast tasks · the `releasable` cue counting them · mine/status visibility. This task = messages only.

Parity invariant: edit canonical add-method/tooling/add.py; mirror byte-identical to
.add/tooling/add.py + _bundled (prepare_bundle.py); re-pin ENGINE_MD5.
Green: test_v8_1_orphan_guard (non-fast path) · test_tree_parity · test_shared_engine_pin.
The `infos`/`informed` keys are ADDITIVE — existing `warnings`/`warned`/`failed`/`checks` unchanged.
```

`Least-sure flag surfaced at freeze:` [spec] silent vs. soft-info on a fast standalone task — RESOLVED at freeze: **soft-info** (human chose a visible affirmation over silence). Cost paid: a new additive `infos` tier in cmd_check's text + --json output.
Status: FROZEN @ v1 — approved by Tin Dang (AskUserQuestion freeze, soft-info), 2026-06-25.
<!-- The freeze IS the one approval. Approved -> Status: FROZEN @ vN — approved by <name>.
     Changing a frozen contract = change request back to SPECIFY. -->

---

## 4 · TESTS — failing-first (red)

Plan: `test_standalone_fast_lane.py` —
  (a) `new-task t --fast` in a no-milestone project → stdout affirms standalone lane, contains NO "intake" nudge;
  (b) `check --json` → `t` is NOT in `warnings` (warned not inflated) AND appears in the new `infos` array (`informed` ≥ 1);
  (c) regression: `new-task u` (no --fast) → still nudges ("not attached"), and `check` still WARNs `u` (in `warnings`).
Red first: today (a) fails (fast tasks get the nudge), (b) fails (no `infos` tier exists + fast tasks are warned).
Tests live in: `add-method/tooling/test_standalone_fast_lane.py` · MUST run red before Build.

---

## 5 · BUILD — AI writes code

Scope (may touch): `add-method/tooling/add.py` · `.add/tooling/add.py` · `add-method/src/add_method/_bundled/tooling/add.py` · `add-method/tooling/engine_pin.py` · `add-method/tooling/test_standalone_fast_lane.py` · `add-method/tooling/test_ground_wiring.py`
Note: test_ground_wiring.py added at build — its check-json key-set surface lock legitimately grows for the approved soft-info tier (declared before re-anchoring the state.json scope anchor).
Code lives in: `add-method/tooling/` (+ mirrored trees)   ·   Constraints: change no existing test, no contract; allow-list packages only; non-fast orphan path untouched.

---

## 6 · VERIFY — evidence + gate

- [x] all tests pass · coverage held · no test or contract altered during build — full tooling suite 1748/0; the frozen §3 untouched (the test_ground_wiring key-set lock is a SURFACE lock, legitimately grown by the approved soft-info contract, not a weakening to pass)
- [x] green was EARNED — the test asserts observable behavior (affirmation message present · `infos` populated · `warned`/`warnings` not inflated · non-fast orphan path intact), no overfit/vacuous asserts
- [x] no exposed secrets, injection openings, or unexpected dependencies — change is print strings + one in-memory `infos` list; no I/O, no deps, no security surface

Build expectations (from §1 Accept + §3 CONTRACT): in a milestone-less project, `new-task x --fast` prints the standalone-lane affirmation (no intake nudge) and `check` emits a soft INFO for `x` (in `infos`, NOT `warnings`); the non-fast orphan path is unchanged — confirmed by `test_standalone_fast_lane.py` red→green plus `test_v8_1_orphan_guard`, `test_tree_parity`, `test_shared_engine_pin` all green.

### GATE RECORD
Outcome: PASS
Reviewed by: Tin Dang (auto-gate on evidence, autonomy: auto) · date: 2026-06-25
OBSERVE: [SDD · open] the `check --json` schema is a SURFACE LOCK (test_no_new_json_key_on_check) — a deliberate, frozen-contract tier addition is the only legal way it grows; worth a one-line note in the build guide so future schema additions declare the lock in §5 up front.
<!-- A security finding is ALWAYS HARD-STOP. Record exactly one outcome — no silent pass.
     OBSERVE (optional): one `[SPEC · open]` or competency-delta line here if the loop taught the foundation something. -->
