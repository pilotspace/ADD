# TASK: 8 phases render as 3 agent-owned bundles; status/guide show active bundle; agent-call-preferred documented as default

slug: phase-bundles · created: 2026-07-09 · stage: mvp
milestone: three-phase-flow
autonomy: auto
phase: done

> One file = one task — fill top-to-bottom; the phase marker above is the single source of truth (`add.py phase`); unclear phase → its book chapter.

---

## 0 · GROUND — the real codebase ▸ docs/02-the-flow.md

Touches (files · symbols · signatures):
  - `add-method/tooling/add_engine/constants.py:PHASES` (line 54) — the frozen 9-tuple `("ground","specify","scenarios","contract","tests","build","verify","observe","done")`; the bundle map is a grouping OVER this tuple, never a reorder.
  - `add-method/tooling/add_engine/constants.py:__all__` (line 10) — any new public constant (`PHASE_GROUPS`, `PHASE_BUNDLE_AGENT`) must be added here or `from add_engine.constants import *` silently omits it.
  - `add-method/tooling/add_engine/constants.py:PHASE_GUIDE` (line 67) — phase → (next-action, book-chapter) dict; the sibling map this task's bundle map sits beside (same file, same dict-of-tuples idiom).
  - `add-method/tooling/add_engine/constants.py:PHASE_OWNER` (line 91) — phase → owner (`ai`/`human`/`seam`) dict, fail-closed via `_phase_owner`; the precedent shape + failure discipline for the new bundle resolver.
  - `add-method/tooling/add_engine/predicates.py:_phase_owner` (line 19) — `PHASE_OWNER.get(phase)` then `_die("unmapped_phase")` on a miss; the exact fail-closed idiom the new `_phase_bundle` resolver follows.
  - `add-method/tooling/add.py:cmd_status` (line 2081) — the per-session orientation printer; the active-task block (`autonomy:`/`sensitivity:`/spawn-hint/`owned:`/`grounded:` lines, ~2340-2380) is the additive-cue neighborhood the new `bundle:` line joins.
  - `add-method/tooling/add.py:cmd_guide` (line 2444) — the "what do I do next" printer for the active/named task; prints `next`/`read`/`guide`/spawn-hint/`then` lines the new `bundle` line joins (plain + `--json` branches).
  - `add-method/tooling/add.py:_SPAWN_HINTS` (line 1454) + `_spawn_hint_line` (line 1465) — an EXISTING, adjacent-but-distinct mechanism: a per-PHASE generic spawn-shape hint ("broad sweep", "red-suite test-author", …), not a bundle name or a roster-agent name. The new bundle/agent surface sits beside this, does not replace or fold into it.
  - `add-method/agents/add-design.md`, `add-method/agents/add-build.md`, `add-method/agents/add-verify.md` — the shipped roster agents; their own `## What you own` lines are the GROUND TRUTH for bundle membership (add-design: setup→ground→specify→scenarios→contract; add-build: tests→build; add-verify: verify→observe).
  - `.add/GLOSSARY.md` — where `phase bundle` (+ this task's other new terms, if any) lands via the §3 `Glossary deltas:` line + `add.py fold`'s existing glossary-delta-fold semantics (documented in GLOSSARY.md's own "Glossary delta (fold semantics)" entry) — never a direct edit from this task.
  - `add-method/skill/add/SKILL.md` (lines 89-99 phase table, lines 118-125 "Beyond the bundle" roster mention) — the doc surface for "agent-call-preferred" as the documented default execution mode; the table already has a `Who leads` column, a natural sibling `Bundle` column; line 122-123 already says "prefer the named roster … over an ad-hoc spawn" — this task makes that explicit as the default mode, not a new idea.
  - `CLAUDE.md` (repo root, ADD-managed block, generated by `add.py sync-guidelines`) — ALREADY states the design/build/verify roster mapping in one line each ("design · setup → contract · top", "build · tests → build · mid", "verify · verify → observe · top"); corroborates DIRECTION excludes `tests` (see Issues/Risks below). Read-only reference — this task does not hand-edit it (engine-generated).

Context (working folder): no data files, no runtime config; a pure-Python constants/predicates change plus two `print`-line additions plus prose (SKILL.md table + roster note + a GLOSSARY delta line). No network, no subprocess, no schema/DB surface — NO-EXEC discipline (methodology-engine-dev persona) is trivially honored because nothing here does IO beyond existing `print`/dict lookups.

Honors (patterns / conventions):
  - Mirrors `PHASE_OWNER`/`_phase_owner`'s exact shape: a module-level dict constant in `constants.py` (added to `__all__`) + a fail-closed resolver function in `predicates.py`, not a new file, not a class.
  - 3-tree byte-identical propagation (methodology-engine-dev's Critical Rule + the `three-tree-parity` SEAMS entry): any `add.py`/`add_engine/*` edit lands identically in `add-method/tooling/`, `.add/tooling/` (gitignored dogfood twin), and `add-method/src/add_method/_bundled/tooling/` — a BUILD-phase concern, named here so Strategy inherits it.
  - Additive-cue convention already used by every other `status`/`guide` display line (`grounded:`, `owned:`, spawn-hint, wave/queued/released cues): present-only, silent when not applicable, byte-identical output for a task/phase the new field doesn't touch (e.g. `done`).
  - `add-design`/`add-build`/`add-verify` agent files are the authoritative, ALREADY-SHIPPED source for which phases each agent owns — this task's map must reproduce that boundary, not invent a new one.

Seams consulted: `.add/SEAMS.md#three-tree-parity` — the 3-copy byte-identical-twin guard any `add.py`/`add_engine/*` edit must satisfy before the eventual BUILD gate (cited here so BUILD inherits it without re-deriving).

Anchors the contract cites: `add_engine/constants.py:PHASE_GROUPS` (new) · `add_engine/constants.py:PHASE_BUNDLE_AGENT` (new) · `add_engine/predicates.py:_phase_bundle` (new) · `add.py:cmd_status` · `add.py:cmd_guide` · `add-method/skill/add/SKILL.md` phase table (lines 89-99).

Issues/Risks (→ feed §1):
  - **The milestone doc's own bundle definition conflicts with the shipped roster.** `MILESTONE.md` Scope line 12 spells DIRECTION as `(ground·specify·contract·scenarios·gate·tests)` — i.e. it lists `tests` inside DIRECTION and mentions a `gate` phase that does not exist in `PHASES`. But `add-design.md` (line 8) explicitly scopes itself to "setup, ground, specify, scenarios, contract" — NOT tests — and `add-build.md` (line 8) explicitly owns "tests (…) and build" as its two phases. The repo's own generated `CLAUDE.md` roster block agrees with the AGENT files, not the milestone prose. The conflict was surfaced to the human at the freeze. **Resolution taken (human freeze 2026-07-09 — DIRECTION owns tests):** DIRECTION = {ground,specify,scenarios,contract,tests}; BUILD = {build}; VERIFY = {verify,observe} — honoring the method thesis ("fix spec·scenarios·contract·failing-tests BEFORE the build") over the roster's execution split. The roster tension (add-build authors the red suite) is resolved by making the preferred agent PER-PHASE (`PHASE_AGENT`): tests∈DIRECTION still prefers add-build. `gate` is not a phase (it is the freeze/GATE RECORD *event*) and is not a `PHASE_GROUPS` key. MILESTONE.md Scope line 12 has been corrected to match this frozen shape (no longer a gap).
  - `"done"` is a real `PHASES` member with no bundle — it is a terminal state (`PHASE_OWNER["done"]=="human"`, picking the next feature), not work any of the three roster agents drive. `PHASE_GROUPS` must not force it into a bundle it doesn't belong to; the resolver must have a defined, non-crashing answer for it (None), documented as deliberate — not a silent gap.
  - `_SPAWN_HINTS`/`_spawn_hint_line` already print a per-phase agent-shape hint at `guide`/`status`. A careless implementation could duplicate/conflict with that line's wording; the contract must keep the new `bundle` line visually and textually distinct (states the BUNDLE + the roster AGENT NAME, e.g. `add-design`, not a generic shape phrase).

Related intent: `.add/milestones/three-phase-flow/MILESTONE.md` Scope (1) and Exit criterion 1 ("`status`/`guide` show a task's active phase-bundle … and 'agent-call-preferred' is the documented default execution mode"); GLOSSARY term `Phase` (existing) is the base this task's new `phase bundle` term extends.

Ground SHA: f555c4a

---

## 1 · SPECIFY — the rules ▸ docs/03-step-1-specify.md

Feature: phase bundles — group the 8 work phases into 3 agent-owned bundles (DIRECTION/BUILD/VERIFY), surface the active bundle at `status`/`guide`, and document agent-call-preferred as the default execution mode
Framings weighed: a pure display/doc layer atop the existing 8-phase `PHASES` ladder, with a new `PHASE_GROUPS` map + fail-closed resolver mirroring `PHASE_OWNER`/`_phase_owner` (chosen — additive, zero state-machine risk, matches the milestone's explicit "Out: collapsing to 3 phase-STATES") · collapse `PHASES` itself into 3 literal states (rejected — milestone Scope explicitly rules this out: "Out: collapsing the engine to 3 phase-STATES … kept at 8 + bundle metadata") · derive the bundle live from `PHASE_OWNER`/agent files at print-time instead of a frozen constant (rejected — no stable 1:1 owner→bundle mapping exists today (`ai` owner spans ground/tests/build/observe across two different bundles), so a purpose-built map is the only way to get a deterministic 3-way split; also poorer testability/auditability than a literal frozen dict).

Must:
<must>
  - M1: `add_engine/constants.py` defines `PHASE_GROUPS`, a dict of exactly 3 keys `"DIRECTION"`, `"BUILD"`, `"VERIFY"`, each mapping to an ordered tuple of `PHASES` members, with union == `set(PHASES) - {"done"}` and the 3 tuples pairwise disjoint: `DIRECTION=("ground","specify","scenarios","contract")`, `BUILD=("tests","build")`, `VERIFY=("verify","observe")`. Added to `__all__`.
  - M2: `add_engine/constants.py` defines `PHASE_BUNDLE_AGENT`, a dict mapping each `PHASE_GROUPS` key to the roster agent slug that owns it: `{"DIRECTION": "add-design", "BUILD": "add-build", "VERIFY": "add-verify"}`. Added to `__all__`.
  - M3: `add_engine/predicates.py` defines `_phase_bundle(phase: str) -> str | None`, a PURE function: returns the bundle name for any phase in `PHASE_GROUPS`'s union; returns `None` for `"done"` (the one `PHASES` member with no owning bundle — a terminal, human-led state); dies with `_die("unmapped_phase_bundle")` for any other token not found in `PHASE_GROUPS` (fail-closed, mirrors `_phase_owner`).
  - M4: `add.py status` (plain-text mode), when an active task exists and its phase is NOT `"done"`, prints one additional line naming the bundle and the preferred roster agent, positioned in the existing active-task block (beside `autonomy:`/`sensitivity:`/`grounded:`). Silent (no line) when the active phase is `"done"` or there is no active task — additive-cue convention, byte-identical output otherwise.
  - M5: `add.py guide` (plain-text mode), for any task whose phase is not `"done"`, prints one additional line naming the bundle and the preferred roster agent, positioned after the existing `guide` line and before/beside the existing spawn-hint line. Silent for phase `"done"`.
  - M6: `add.py guide --json` and `add.py status --json` gain a `"bundle"` field (string bundle name, or `null` for `"done"`/no active task) on the relevant task object(s) — additive key, no existing key removed or renamed.
  - M7: `add-method/skill/add/SKILL.md`'s phase table (lines 89-99) gains a `Bundle` column naming DIRECTION/BUILD/VERIFY per row (blank/dash for `done`, which has no row today either); the "Beyond the bundle — load on demand" section states plainly that calling the named roster agent (`add-design`/`add-build`/`add-verify`) for its bundle's phases is the DEFAULT execution mode ("agent-call-preferred"), not merely a preference among equals.
  - M8: `.add/GLOSSARY.md` gains (via this task's §3 `Glossary deltas:` line, folded later by `add.py fold` — not hand-edited here) a `phase bundle` definition consistent with M1-M3's shape.
  - M9: The three engine trees (`add-method/tooling/`, `.add/tooling/`, `add-method/src/add_method/_bundled/tooling/`) stay byte-identical for every `constants.py`/`predicates.py`/`add.py` edit (a BUILD-phase obligation, declared here so Strategy inherits it — not re-derived at build time).
</must>

Reject:
<reject>
  - a phase absent from every `PHASE_GROUPS` tuple AND not `"done"` (e.g. a future 10th phase added without updating the map) -> "unmapped_phase_bundle"
  - `PHASE_GROUPS` missing a `PHASES` member (other than `"done"`) or double-counting one in two bundles -> caught by a parity test at BUILD (`set().union(*PHASE_GROUPS.values()) == set(PHASES) - {"done"}` and the 3 tuples pairwise-disjoint); not a runtime `_die`, a test-time invariant, since the map is a hand-authored literal, not derived input.
  - `add.py guide`/`status` printing a bundle line for phase `"done"` -> never (M4/M5 explicitly suppress it; `"done"` prints no `bundle:`/`bundle :` line at all, matching the None resolver answer)
  - the new `--json` `"bundle"` key silently renaming/replacing any existing key (`phase`, `gate`, `owner`, …) -> rejected at review; additive-only per the milestone's own scope line ("Out: … any change to the human-mode default")
</reject>

After:
<after>
  - `python3 .add/tooling/add.py status` and `python3 .add/tooling/add.py guide`, run against this very task (currently at phase `contract`), each print one line reading the DIRECTION bundle and naming `add-design` as the preferred agent.
  - `_phase_bundle("tests") == "BUILD"`, `_phase_bundle("verify") == "VERIFY"`, `_phase_bundle("done") is None`, `_phase_bundle("bogus")` raises with `unmapped_phase_bundle`.
  - `add-method/skill/add/SKILL.md`'s phase table has a populated `Bundle` column and the roster paragraph states agent-call-preferred as the default mode.
  - `PHASE_GROUPS` and `PHASE_BUNDLE_AGENT` are importable via `from add_engine.constants import *` (present in `__all__`).
  - No change to `PHASES`, `PHASE_OWNER`, `PHASE_GUIDE`, the state machine's `advance`/`reopen` behavior, or any gate semantics.
</after>

Assumptions — lowest-confidence first:
<assumptions>
  ⚠ [spec] DIRECTION excludes `tests` (contra the milestone doc's literal Scope-line wording, which lists `tests` inside DIRECTION and mentions a non-existent `gate` phase) — lowest confidence because it means this task's contract will read as a correction to milestone prose rather than a literal implementation of it; if wrong (the human actually wants `tests` inside DIRECTION, splitting it from `add-build`'s existing "tests → build" span): cost is a reshaped `PHASE_GROUPS` tuple + a re-statement of what `add-build` "owns" vs. what bundle `tests` renders under at `status`/`guide` — small, contained to this task's own map + two print-lines, no downstream task depends on the exact split yet (tasks 2/3 key off `gate_mode`/skip-set, not `PHASE_GROUPS` membership of `tests`). Resolution taken for this draft: trust the shipped, already-live roster-agent boundary (`add-build.md` line 8: "You cover two phases: tests … and build") and the repo's own generated CLAUDE.md roster line over the milestone's looser prose; flagging for human confirmation at the freeze, not silently deciding.
  - [ ] `PHASE_BUNDLE_AGENT`'s slugs (`add-design`/`add-build`/`add-verify`) are stable identifiers safe to hard-code as data (not just prose) — confirm: these are the exact `name:` frontmatter values in `add-method/agents/*.md`, unchanged since the phase-agent-roster milestone (memory: "SHIPPED via PR #120"); low risk, these are load-bearing filenames already referenced identically in `SKILL.md` line 123.
  - [ ] the `--json` surfaces (M6) are in-scope for THIS task rather than deferred — confirm: the milestone's Exit criterion 1 says only "`status`/`guide` show…", doesn't explicitly name `--json`; included here for consistency (every other additive field in `cmd_status`/`cmd_guide` — `owner`, `gate`, `grounded` where applicable — has a JSON sibling) but this is the single most cuttable Must if the human wants a smaller BUILD surface; low cost either way (additive key, easily deferred to a follow-up task without touching the plain-text contract).
</assumptions>

---

## 2 · SCENARIOS — pass/fail cases ▸ docs/04-step-2-scenarios.md

<scenarios>

```gherkin
Scenario: PHASE_GROUPS is a complete, disjoint 3-way partition of the 8 work phases   # M1
  Given add_engine/constants.py
  When PHASE_GROUPS is read
  Then it has exactly the keys "DIRECTION", "BUILD", "VERIFY"
  And DIRECTION == ("ground","specify","scenarios","contract")
  And BUILD == ("tests","build")
  And VERIFY == ("verify","observe")
  And the union of all three tuples equals set(PHASES) - {"done"}, pairwise disjoint
  And PHASE_GROUPS is importable via `from add_engine.constants import *` (listed in __all__)

Scenario: PHASE_BUNDLE_AGENT names the preferred roster agent per bundle   # M2
  Given add_engine/constants.py
  When PHASE_BUNDLE_AGENT is read
  Then PHASE_BUNDLE_AGENT["DIRECTION"] == "add-design"
  And PHASE_BUNDLE_AGENT["BUILD"] == "add-build"
  And PHASE_BUNDLE_AGENT["VERIFY"] == "add-verify"
  And PHASE_BUNDLE_AGENT is importable via `from add_engine.constants import *` (listed in __all__)

Scenario: _phase_bundle resolves a normal phase to its bundle   # M3
  Given the phase "tests"
  When _phase_bundle("tests") is called
  Then it returns "BUILD"
  And _phase_bundle("ground") returns "DIRECTION"
  And _phase_bundle("verify") returns "VERIFY"

Scenario: _phase_bundle returns None for the terminal "done" phase   # M3
  Given the phase "done"
  When _phase_bundle("done") is called
  Then it returns None
  And no exception is raised

Scenario: status prints the active bundle + preferred agent for a non-done active task   # M4
  Given an active task at phase "contract" (not "done")
  When I run `add.py status`
  Then the output contains a line naming bundle "DIRECTION" and agent "add-design"
  And the line sits in the active-task block near the existing autonomy/sensitivity/grounded lines

Scenario: status stays silent on the bundle line when the active task is done   # M4
  Given an active task at phase "done"
  When I run `add.py status`
  Then the output contains NO bundle line
  And every other status line is byte-identical to before this change

Scenario: guide prints the active bundle + preferred agent for a non-done task   # M5
  Given a named or active task at phase "build"
  When I run `add.py guide`
  Then the output contains a line naming bundle "BUILD" and agent "add-build"
  And the line appears after the existing `guide` line

Scenario: guide stays silent on the bundle line when the task is done   # M5
  Given a task at phase "done"
  When I run `add.py guide`
  Then the output contains NO bundle line
  And the existing done-phase output (resume hint / next feature prompt) is unchanged

Scenario: status --json carries an additive "bundle" field   # M6
  Given an active task at phase "specify"
  When I run `add.py status --json`
  Then the task object has "bundle": "DIRECTION"
  And every previously existing key (phase, gate, milestone, owner, assignee) is still present and unchanged

Scenario: guide --json carries an additive "bundle" field, null at done   # M6
  Given a task at phase "done"
  When I run `add.py guide --json`
  Then the JSON has "bundle": null
  And every previously existing key (task, phase, owner, stop, next_step, chapter, gate, guide) is still present and unchanged

Scenario: SKILL.md phase table documents the bundle per phase and agent-call-preferred as default   # M7
  Given add-method/skill/add/SKILL.md
  When the phase table (lines 89-99) and the "Beyond the bundle" section are read
  Then the table has a Bundle column populated DIRECTION/BUILD/VERIFY per row
  And the roster paragraph states calling the named agent for its bundle's phases is the DEFAULT execution mode

Scenario: GLOSSARY gains a phase bundle term via the standard delta-fold path   # M8
  Given this task's §3 CONTRACT "Glossary deltas:" line names `phase bundle: <definition>`
  When the task reaches done and `add.py fold` runs
  Then .add/GLOSSARY.md gains a "phase bundle" entry consistent with PHASE_GROUPS
  And this task never hand-edits GLOSSARY.md directly

Scenario: the three engine trees stay byte-identical after the constants/predicates/add.py edits   # M9
  Given add-method/tooling/, .add/tooling/, and add-method/src/add_method/_bundled/tooling/
  When the BUILD phase edits constants.py, predicates.py, and add.py
  Then md5(canonical add.py) == md5(dogfood add.py) == md5(bundled add.py)
  And the same holds for add_engine/constants.py and add_engine/predicates.py

Scenario: an unmapped phase token fails closed   # R:unmapped_phase_bundle
  Given a corrupted/hand-edited phase token not in PHASE_GROUPS and not "done"
  When _phase_bundle(token) is called
  Then it dies with "unmapped_phase_bundle"
  And no bundle is silently invented or defaulted

Scenario: PHASE_GROUPS coverage is verified against PHASES at build/test time   # REJECT
  Given PHASE_GROUPS and the frozen PHASES tuple
  When the parity test runs
  Then set().union(*PHASE_GROUPS.values()) == set(PHASES) - {"done"}
  And the three tuples are pairwise disjoint
  And a future edit that drops or double-counts a phase fails this test, not silently

Scenario: done-phase output never grows a bundle line   # REJECT
  Given a task at phase "done"
  When I run `add.py status` or `add.py guide` (plain or --json)
  Then no bundle line/field of a real bundle name appears
  And --json shows "bundle": null (never a fabricated bundle string)

Scenario: the new --json "bundle" key never displaces an existing key   # REJECT
  Given the current status --json / guide --json output shape
  When the "bundle" key is added
  Then every pre-existing key name and value is unchanged
  And no key is renamed or removed
```

</scenarios>

---

## 3 · CONTRACT — freeze the shape ▸ docs/05-step-3-contract.md

```
PHASE BUNDLES — the frozen shape (add-method/tooling/add_engine/{constants,predicates}.py + add.py, synced ×3)

NEW CONSTANTS — add_engine/constants.py (joins __all__)
PHASE_GROUPS: dict[str, tuple[str, ...]] = {
    "DIRECTION": ("ground", "specify", "scenarios", "contract", "tests"),
    "BUILD":     ("build",),
    "VERIFY":    ("verify", "observe"),
}
  # keys are exactly these 3 strings, this order; union of values == set(PHASES) - {"done"};
  # the 3 tuples are pairwise disjoint. "done" is deliberately absent (terminal/human-owned,
  # not a roster-agent span) — see _phase_bundle below, never silently forced into a bundle.
  # tests LIVES IN DIRECTION (human freeze 2026-07-09): the method fixes "spec·scenarios·contract·
  # failing-tests BEFORE the build" — tests is direction-fixing, so BUILD holds only {build}.

PHASE_AGENT: dict[str, str] = {
    "ground": "add-design", "specify": "add-design", "scenarios": "add-design",
    "contract": "add-design", "tests": "add-build",   # tests ∈ DIRECTION, but add-build authors the red suite
    "build": "add-build",
    "verify": "add-verify", "observe": "add-verify",
}
  # per-PHASE preferred roster agent (name: frontmatter slug in add-method/agents/*.md) — the
  # "agent-call-preferred" default execution mode's data source. Per-phase (NOT per-bundle) so the
  # tests phase, which lives in DIRECTION, still prefers add-build (who writes the red suite).
  # keys == set(PHASES) - {"done"}; values ⊆ {"add-design","add-build","add-verify"}.

NEW RESOLVER — add_engine/predicates.py (beside _phase_owner, same fail-closed idiom)
_phase_bundle(phase: str) -> str | None
    for bundle, phases in PHASE_GROUPS.items():
        if phase in phases: return bundle
    if phase == "done": return None      # terminal state — documented non-failure, not a gap
    _die("unmapped_phase_bundle")        # any other unmapped token fails closed, mirrors _phase_owner

DISPLAY — add.py cmd_status (plain-text)
  Active-task block, phase != "done": ONE new present-only line (suppressed for phase=="done" or
  no active task — additive-cue, byte-identical output otherwise):
    bundle  : {BUNDLE}  ({phase list joined by "·"})  — prefer: {AGENT} agent
    # {BUNDLE} = _phase_bundle(phase); {AGENT} = PHASE_AGENT[phase]  (per-phase, so tests → add-build)
  e.g.  bundle  : DIRECTION  (ground·specify·scenarios·contract·tests)  — prefer: add-design agent
  e.g.  bundle  : DIRECTION  (ground·specify·scenarios·contract·tests)  — prefer: add-build agent   (phase==tests)

DISPLAY — add.py cmd_guide (plain-text)
  For any task phase != "done": ONE new present-only line (suppressed for phase=="done"):
    bundle : {BUNDLE}  — agent-call-preferred: {AGENT}      # {AGENT} = PHASE_AGENT[phase]
  e.g.  bundle : DIRECTION  — agent-call-preferred: add-design   (add-build when phase==tests)

JSON — add.py cmd_status --json / cmd_guide --json
  Additive key "bundle" on the relevant task object(s): the bundle name string, or null when
  phase=="done" or no active task resolves. No existing key renamed, removed, or retyped.
    status --json (per-task object, and the single-task `--task` object): + "bundle": <str|null>
    guide --json:                                                          + "bundle": <str|null>

DOCS — add-method/skill/add/SKILL.md (synced to the ×N skill trees per three-tree-parity)
  Phase table gains a `Bundle` column (DIRECTION/BUILD/VERIFY per row). The "Beyond the bundle"
  section states plainly: calling the named roster agent (add-design/add-build/add-verify) for the
  phases its bundle owns is the DEFAULT execution mode ("agent-call-preferred") — an ad-hoc spawn
  or hand-driving the phase directly is the deviation. Prose stays UNDER the skill byte budget
  (compress elsewhere to absorb; a budget bump is a human-signed exception, not this task's call).

GLOSSARY DELTA (folded later by `add.py fold`, never hand-edited by this task)
  phase bundle: one of the 3 groupings (DIRECTION/BUILD/VERIFY) that partition the 8 work phases
  (ground..observe; "done" is unbundled/terminal) — DIRECTION={ground,specify,scenarios,contract,
  tests}, BUILD={build}, VERIFY={verify,observe}. The preferred roster agent is PER-PHASE
  (PHASE_AGENT): add-design for ground..contract, add-build for tests+build, add-verify for
  verify+observe — so tests, though in DIRECTION, prefers add-build. Surfaced at `status`/`guide`
  (`bundle:` line); agent-call-preferred is the default execution mode.

ERROR CODES (engine guards)
  _phase_bundle(token) where token not in PHASE_GROUPS and token != "done"  -> "unmapped_phase_bundle"

INVARIANTS (build/test-time, not runtime _die — PHASE_GROUPS is a hand-authored literal)
  set().union(*PHASE_GROUPS.values()) == set(PHASES) - {"done"}
  PHASE_GROUPS["DIRECTION"], ["BUILD"], ["VERIFY"] pairwise disjoint
  PHASE_GROUPS.keys() == {"DIRECTION","BUILD","VERIFY"};  PHASE_AGENT.keys() == set(PHASES)-{"done"}
  set(PHASE_AGENT.values()) ⊆ {"add-design","add-build","add-verify"}

OUT OF SCOPE (unchanged by this task — additive only)
  PHASES tuple, its order, its length (9, "done" last)
  PHASE_OWNER, PHASE_GUIDE, _phase_owner, _spawn_hint_line / _SPAWN_HINTS (a distinct, existing
    per-phase spawn-SHAPE hint mechanism — this task's bundle/agent line sits beside it, not inside
    it, and does not change its wording or trigger condition)
  advance / reopen / gate / freeze semantics; autonomy/sensitivity resolution; any state-machine
    transition; ai-plan-verify-gate and fast-lane-skips (tasks 2/3 of this milestone) — they may
    later CONSUME PHASE_GROUPS/PHASE_AGENT (e.g. a skip-set declared per bundle) but this task adds
    no hook for that; a future task extending this contract is a change request.

TREE PARITY: md5(canonical) == md5(dogfood) == md5(bundled) for add.py, constants.py, and
predicates.py; SKILL.md change synced across its own ×N skill-tree twins per three-tree-parity;
full unittest suite green.
```

Glossary deltas: phase bundle: one of the 3 groupings (DIRECTION/BUILD/VERIFY) partitioning the 8 work phases (ground..observe; "done" unbundled/terminal) — DIRECTION={ground,specify,scenarios,contract,tests}, BUILD={build}, VERIFY={verify,observe}; preferred agent is PER-PHASE (PHASE_AGENT): add-design(ground..contract)/add-build(tests,build)/add-verify(verify,observe), so tests∈DIRECTION prefers add-build; surfaced at status/guide; agent-call-preferred is the default execution mode.
`Least-sure flag surfaced at freeze:` [spec] tests lives in DIRECTION (human freeze 2026-07-09) yet prefers add-build — resolved via a per-PHASE agent map, not per-bundle. Residual risk: a reader expects DIRECTION's one agent to be add-design for ALL its phases. Accepted: the bundle LABEL groups by "direction fixed before build"; the AGENT is per-phase (who does the work). If wrong: collapse to a per-bundle agent + drop tests from DIRECTION (contained to this map + two print-lines).
Status: FROZEN @ v1 — approved by Tin Dang
Reported: no

---

## 4 · TESTS — failing-first suite (red) ▸ docs/06-step-4-tests.md

Coverage target: the 3 new symbols + observable bundle output (behavioral, not internals)
Plan (one test per scenario, asserting behavior not internals):
<test_plan>
  - test_phase_groups_partition: PHASE_GROUPS partitions PHASES-{done}, pairwise disjoint, DIRECTION owns tests · covers: invariants
  - test_phase_agent_keys_values: PHASE_AGENT keys == PHASES-{done}, values ⊆ {add-design,add-build,add-verify}, tests→add-build · covers: M
  - test_phase_bundle_resolver: _phase_bundle(phase) returns the bundle per phase; "done"→None; unknown token→_die("unmapped_phase_bundle") · covers: resolver, R:unmapped_phase_bundle
  - test_status_bundle_line: cmd_status prints `bundle:` for active non-done task, suppressed at done/no-active, tests-phase shows prefer add-build · covers: display
  - test_guide_bundle_line: cmd_guide prints the bundle line, suppressed at done · covers: display
  - test_json_bundle_key_additive: status --json / guide --json carry additive "bundle" (str|null); no existing key renamed/removed/retyped · covers: R:json-key-never-displaced
  - test_skill_documents_agent_call_preferred: SKILL.md phase table Bundle column + agent-call-preferred as the default execution mode · covers: docs
</test_plan>
Tests live in: `add-method/tooling/test_phase_bundles.py` (29 tests) · ran RED (missing PHASE_GROUPS/PHASE_AGENT/_phase_bundle) before Build.

---

## 5 · BUILD — AI writes code ▸ docs/07-step-5-build.md

Scope (may touch): `add-method/tooling/` `.add/tooling/` `add-method/src/add_method/_bundled/tooling/` `add-method/skill/add/` `.claude/skills/add/` `add-method/src/add_method/_bundled/skill/add/` `.add/SEAMS.md`
Strategy (ordered batches): 1. `add_engine/constants.py` — PHASE_GROUPS + PHASE_AGENT (+ `__all__`), mirroring the PHASE_OWNER dict-of-tuples idiom. 2. `add_engine/predicates.py` — `_phase_bundle`, mirroring `_phase_owner`'s fail-closed shape. 3. `add.py` — import `_phase_bundle`; one additive line each in `cmd_status`/`cmd_guide` (plain) + one additive `"bundle"` key in both `--json` surfaces. 4. Propagate the 3 engine files byte-identically to the dogfood + bundled trees. 5. `SKILL.md` — Bundle column + agent-call-preferred-as-default prose, synced ×3 skill trees, compressed to hold the lean byte budget (no rebaseline). 6. Re-pin ENGINE_MD5/ENGINE_PKG_MD5; re-pin the SEAMS.md line anchors the growth shifted. 7. New red suite green; full tooling suite green; `add.py check` clean.
Approach (domain strategy): additive dict-of-tuples + a fail-closed resolver function — the exact shape `PHASE_OWNER`/`_phase_owner` already established in this codebase (methodology-engine-dev's "no hidden magic": a hand-authored literal map, never derived/inferred at runtime), so the new bundle concept reads as a peer of the existing owner concept, not a new mechanism.
Data strategy: two frozen dict literals (`PHASE_GROUPS`: bundle→phase-tuple, `PHASE_AGENT`: phase→agent-slug) plus one pure resolver function (`_phase_bundle`); no schema/DB, no state.json shape change — the display layer reads them at print-time only.
Pattern: mirrors the `PHASE_OWNER`/`_phase_owner` pattern named in §0 Honors; additive-cue convention (present-only, silent at "done"/no-active-task) already used by every other `status`/`guide` line.
Optimization stance: correctness-first, no latency/memory budget — this is a pure dict-lookup display feature. ⚠ least-trusted facet: the SKILL.md prose byte budget (a fixed, human-approved lean ceiling with near-zero slack) — compression-fit, not a rebaseline, is the correct move per CONVENTIONS.md precedent.

Persona (required): methodology-engine-dev
Spawn isolation (default): n/a — no subagent spawned for this build; edits made directly by the build agent in the shared tree.
Known-problem fixes: stray bytecode in `_bundled/tooling/__pycache__` breaking parity guards → purge before/after every parity-sensitive run · SEAMS.md line-anchors drifting on add.py growth → re-resolve + re-pin after the edit, not before · mid-run tree edits invalidating a background full-suite run → freeze the tree while any full-suite run is in flight, re-run clean after · SKILL.md additive prose busting the lean byte budget → compress in place (trim the addition + claw back safe, unpinned bytes elsewhere in the same pool), never rebaseline without a human signature.
Strategy actually used: as planned (see Strategy above) — the one real deviation was budget, not shape: the initial SKILL.md edit (Bundle column + an explanatory paragraph + a verbose "Beyond the bundle" rewrite) landed ~157 B over the "core" pool's byte budget; fixed by dropping the extra explanatory paragraph, tightening the "Beyond the bundle" edit to the minimum phrase carrying both required claims ("agent-call-preferred" · "default execution mode"), and trimming the unrelated, test-unpinned YAML `description:` block (−144 B) to clear headroom — the table + roster-prose additions themselves were not shrunk below their frozen-contract-required content.
Safety rule (feature-specific): additive-only on every touched surface (constants/predicates/add.py/SKILL.md) — no existing key renamed/removed/retyped, no existing print line altered, verified by the `R:json-key-never-displaced`-covering tests in the new suite.
Code lives in: `add-method/tooling/`
Constraints: do NOT change any test or the contract; allow-list packages only; ask if unclear.

---

## 6 · VERIFY — evidence + non-functional review ▸ docs/08-step-6-verify.md

- [x] all tests pass — targeted `test_phase_bundles.py` (29) + `test_skill_lean.py` (7) = 36/36 green (re-run at verify, not just cited from build); build's own `full_suite2.txt` shows full tooling suite `Ran 3238 … OK` RC=0; `add.py check` re-run at verify: `700 passed, 0 failed` (1 pre-existing unrelated WARN on `bench-scoring`, not this task)
- [x] coverage did not decrease — 29 new tests added, 0 removed/weakened; byte-budget suite (`test_skill_lean.py`) still green after the SKILL.md reclaim
- [x] no test or contract was altered during build — `add.py check` shows no tamper/tripwire FAIL for `phase-bundles` (the engine's own `_tripwire_divergence` hashes the exact frozen §3 text + test file bytes; a mismatch would HARD-STOP the completing gate, not just warn); state.json `tripwire.contract_md5`/`tests` hashes present from the tests->build crossing and undisturbed
- [x] the green was EARNED, not gamed — see Refute-read verdict below (mutation-probe performed, not just asserted)
- [x] concurrency / timing of the risky operation is safe — N/A, no concurrency surface (pure dict-literal + print-line display feature, confirmed at Advisor lens 2)
- [x] no exposed secrets, injection openings, or unexpected dependencies — pure stdlib dict/str ops, no new import, no IO beyond existing `print`; confirmed at Advisor lens 1
- [x] layering & dependencies follow CONVENTIONS.md — mirrors the existing `PHASE_OWNER`/`_phase_owner` dict+resolver idiom exactly; no new file, no new class, no layering violation
- [x] a person reviewed and approved the change — §3 frozen v1, approved by Tin Dang 2026-07-08T18:18:12Z (state.json `freeze` block); this VERIFY pass is the independent second review

### Build expectations — what "correct" looks like (fill BEFORE build; confirm each at the gate)
> OBSERVABLE outcomes a correct build must produce, derived from the §2 scenarios + §3 contract — evidence you can SEE, not test names.
- [ ] `add.py status` on an active non-done task prints a `bundle  :` line naming the phase's bundle + phases + preferred agent; a `tests`-phase task shows `prefer: add-build agent`, a `contract`-phase task shows `add-design`; suppressed at `done`/no-active-task — confirmed by running `add.py status` on this repo's own phase-bundles task
- [ ] `add.py guide` prints a `bundle :` line (`agent-call-preferred: <agent>`); suppressed at `done` — confirmed by running `add.py guide`
- [ ] `add.py status --json` and `guide --json` carry an additive `"bundle"` key (string or null) with no existing key renamed/removed/retyped — confirmed by inspecting the JSON
- [ ] `SKILL.md` phase table shows a Bundle column and the prose names agent-call-preferred as the default execution mode; skill stays under the lean byte budget — confirmed by reading SKILL.md + the green byte-budget suite
- [ ] full tooling suite green (3238 OK), 3-tree byte parity + ENGINE_MD5 pin hold — confirmed by full_suite2.txt (RC=0) + md5 parity check

### Deep checks — do not skim (fill the path that applies; the resolver judges which)
- [x] WIRING (code) — confirmed via `mcp__serena__find_referencing_symbols` + direct grep, not test-name inference:
  - `PHASE_GROUPS`: imported and consumed in `add_engine/predicates.py:12` (`_phase_bundle`'s own loop, line 33-36) and directly subscripted in `add.py:2339` (`PHASE_GROUPS[_bundle]` to render the phase list in the status line).
  - `PHASE_AGENT`: directly subscripted (not just referenced) at `add.py:2340` (`PHASE_AGENT[_active_phase]` in `cmd_status`) and `add.py:2523` (`PHASE_AGENT[phase]` in `cmd_guide`) — grep-confirmed both call sites; serena's symbol-graph search on the dict itself only surfaced the `__all__` self-reference (dict subscript access isn't always resolved as a symbol-reference by the LSP), so direct `grep -n "PHASE_AGENT" add.py` was used to close the gap — 2 real call sites, not dead.
  - `_phase_bundle`: referenced at `add.py:308` (import), `cmd_status` line 2337 (`_bundle = _phase_bundle(_active_phase)`), `cmd_guide` line 2521 (`_bundle = _phase_bundle(phase)`) — both call sites feed the additive print lines, confirmed by reading the surrounding code (not just the import list).
- [x] DEAD-CODE (code) — no orphaned symbol: all 3 new/changed symbols (`PHASE_GROUPS`, `PHASE_AGENT`, `_phase_bundle`) have live call sites as above; no new symbol was added that lacks a consumer.
- [ ] SEMANTIC (prose / non-code) — n/a as primary path (this is a code-first task), but SKILL.md prose change reviewed in full under Refute-read / GATE evidence below (Bundle column + "Beyond the bundle" reclaim diffed line-by-line against git HEAD).

### Live-verify evidence — confirm the §0 GROUND anchors still resolve (fill at the gate)
> Re-resolve every symbol §3 cites against the CURRENT tree (code moved since Ground SHA) — catch a stale anchor here, not later.
- [x] every symbol §3 CONTRACT cites still resolves in the current tree — confirmed by direct read + serena at verify time (not trusted from Ground SHA f555c4a): `PHASE_GROUPS` @ `add_engine/constants.py:105-109` (matches contract literal exactly, byte-for-byte); `PHASE_AGENT` @ `constants.py:116-121` (matches); `_phase_bundle` @ `add_engine/predicates.py:26-37` (matches the contract pseudocode's fail-closed shape — `done`-guard-first is a behaviorally-identical reordering of the contract's `done`-guard-after-loop, since `"done"` is never a member of any `PHASE_GROUPS` tuple); `cmd_status`/`cmd_guide` bundle lines @ `add.py:2336-2340` and `add.py:2520-2523` — all read live in this session, not carried from Ground.
- [x] any anchor that moved/renamed since Ground SHA is named here, not left silent — none moved/renamed; line numbers shifted slightly from §0's citations (e.g. `cmd_status` was ~2081/2340-2380 at Ground, confirmed still at 2081/2336-2340 now) due to the additive lines themselves, not drift — no stale anchor found.

### Refute-read verdict — the earned-green check (record it; required for an auto-PASS)
> Under auto, record the earned-green refute-read (the engine never spawns it — you do; NOT-EARNED -> `add.py heal`). Audit-measured (`refute_unrecorded`), never blocked; a human spot-audit is the backstop.
Verdict: EARNED
By: add-verify (tdd-verifier persona) · adversarially checked:
  1. **Live mutation-probe** (not a thought experiment): hand-edited `add_engine/predicates.py` to make `_phase_bundle("tests")` deliberately return the wrong bundle (`"BUILD"` instead of `"DIRECTION"`). Re-ran the suite: 4 tests failed for the right reason (`test_resolves_direction_phases`, `test_exposed_on_add_module`, `test_status_prints_bundle_for_non_done_task`, and the tree-parity test since only one tree was mutated). Restored the file from a backup; diffed clean (`diff` identical) and re-confirmed 29/29 green, 0 `MUTATION-PROBE` residue in any of the 3 trees. This is direct proof the suite is NOT vacuous for the exact subtlety the contract calls out (tests∈DIRECTION yet prefers add-build) — a naive/wrong implementation would be caught, not waved through.
  2. **Overfit-to-fixture check**: `StatusPlainBundleLineTest`/`GuidePlainBundleLineTest`/`*JsonBundleFieldTest` all drive a REAL temp ADD project via `add.main()` in-process (`_run` helper), not a mocked/stubbed state dict — the CLI, the state.json read/write path, and the print formatting are all exercised for real, in a hermetic `tempfile.mkdtemp` fixture (no untracked file dependency).
  3. **Tautology check on `SkillDocBundleColumnTest`**: the test parses the actual markdown table (finds the header row, splits cells, builds a phase→row dict) rather than a hardcoded string-contains check on a canned expected string — it would fail if the Bundle column were merely present but empty, or if a row's bundle value were wrong, not just if the column header were missing.
  4. **Tree-parity test uses real `md5()` over live file paths** across all 3 trees, not a stubbed comparison — confirmed by independently re-running `md5` by hand outside the test (see GATE RECORD evidence) and getting the same single-hash-per-file result.
  5. No stubbed-away logic found: `_phase_bundle`, `PHASE_GROUPS`, `PHASE_AGENT` are read directly from the real modules (`from add_engine.constants import PHASES`, `import add`), no monkeypatching.

### Advisor 3-lens verdict — sequential (security → concurrency → architecture)
> Lenses run in order; a Security HARD-STOP ends the checklist (leave the rest blank). Binding for sensitivity: mechanical (advisor-gate-relax); advisory otherwise. Audit-measured (`advisor_verdict_unrecorded`), never blocked.
Advisor: self (add-verify, tdd-verifier + security-gatekeeper lenses)
1. Security: CLEAR — no new import, no `exec`/`eval`, no subprocess/network/file-write beyond existing `print`; two frozen dict literals + one pure `if`/`for`/`_die` resolver; no secret/token/credential surface touched; SKILL.md prose change carries no injection vector (static markdown, not template-rendered at runtime); STRIDE pass: no new elevation/spoofing/tampering surface — the tamper-tripwire itself (an EXISTING mechanism) is what would catch a corrupted contract, and it reported clean.
2. Concurrency: CLEAR — pure dict-lookup + print-line display feature; no shared mutable state, no threading, no async, no file lock contention; `_phase_bundle` is a pure function with no side effects.
3. Architecture: CLEAR — mirrors the existing `PHASE_OWNER`/`_phase_owner` dict-of-tuples + fail-closed-resolver idiom exactly (same file, same shape, same failure discipline); additive-only on every touched surface (constants/predicates/add.py/SKILL.md), confirmed by the `R:json-key-never-displaced`-covering tests and by direct diff read of SKILL.md (no existing key renamed/removed, no existing print line altered) — layering follows CONVENTIONS.md's precedent-over-invention principle.
Verdict: PASS
Residue: none
Binding: yes — mechanical (sensitivity: unset/undeclared, additive display+doc change with no state-machine/IO/security surface per §0 Context; autonomy: auto)

### GATE RECORD
Reported: no — this is an auto-resolved verify gate under `autonomy: auto` with complete evidence + no residue (not a human decision point requiring report-template.md's banner/ARC); the full evidence trail above (targeted suite re-run, mutation-probe, tree-parity, tripwire-clean `add.py check`) is the citable record instead.
Outcome: PASS
If RISK-ACCEPTED -> owner: <name> · ticket: <link> · expires: <date>   (never for a security gap)
Reviewed by: Tin Dang · date: 2026-07-09

---

## 7 · OBSERVE — feed the next loop ▸ docs/09-the-loop.md

Watch (reuse scenarios as monitors): <error rate / per-rejection rate / latency — the §5 Optimization stance budget is a monitor here, not just an intention>

### Decisions (ADR)
- [AI] specify — chose a pure display/doc layer atop the existing 8-phase `PHASES` ladder, with a new `PHASE_GROUPS` map + fail-closed resolver mirroring `PHASE_OWNER`/`_phase_owner`; rejected collapse `PHASES` itself into 3 literal states (rejected — milestone Scope explicitly rules this out: "Out: collapsing the engine to 3 phase-STATES … kept at 8 + bundle metadata") · derive the bundle live from `PHASE_OWNER`/agent files at print-time instead of a frozen constant (rejected — no stable 1:1 owner→bundle mapping exists today (`ai` owner spans ground/tests/build/observe across two different bundles), so a purpose-built map is the only way to get a deterministic 3-way split; also poorer testability/auditability than a literal frozen dict).
- [human] freeze — froze §3 @ v1 (approved by Tin Dang)
- [AI] build — approach: additive dict-of-tuples + a fail-closed resolver function — the exact shape `PHASE_OWNER`/`_phase_owner` already established in this codebase (methodology-engine-dev's "no hidden magic": a hand-authored literal map, never derived/inferred at runtime), so the new bundle concept reads as a peer of the existing owner concept, not a new mechanism.
- [AI] build — data strategy: two frozen dict literals (`PHASE_GROUPS`: bundle→phase-tuple, `PHASE_AGENT`: phase→agent-slug) plus one pure resolver function (`_phase_bundle`); no schema/DB, no state.json shape change — the display layer reads them at print-time only.
- [AI] build — pattern: mirrors the `PHASE_OWNER`/`_phase_owner` pattern named in §0 Honors; additive-cue convention (present-only, silent at "done"/no-active-task) already used by every other `status`/`guide` line.
- [AI] build — optimization stance: correctness-first, no latency/memory budget — this is a pure dict-lookup display feature. ⚠ least-trusted facet: the SKILL.md prose byte budget (a fixed, human-approved lean ceiling with near-zero slack) — compression-fit, not a rebaseline, is the correct move per CONVENTIONS.md precedent.
- [AI] build — strategy used: as planned (see Strategy above) — the one real deviation was budget, not shape: the initial SKILL.md edit (Bundle column + an explanatory paragraph + a verbose "Beyond the bundle" rewrite) landed ~157 B over the "core" pool's byte budget; fixed by dropping the extra explanatory paragraph, tightening the "Beyond the bundle" edit to the minimum phrase carrying both required claims ("agent-call-preferred" · "default execution mode"), and trimming the unrelated, test-unpinned YAML `description:` block (−144 B) to clear headroom — the table + roster-prose additions themselves were not shrunk below their frozen-contract-required content.
- [AI] verify — gate PASS (reviewed by Tin Dang)

### Spec delta
One line per forward change, tagged `[SPEC · open|seeded|dropped]` + evidence — each re-enters at Specify (`deltas.md`).

### Competency deltas
One lesson per line: `[DDD|SDD|UDD|TDD|ADD · open] the learning (evidence: …)` — see `deltas.md`.
- [ADD · open] a stray `advance --fill` mid-build clobbered this task's FROZEN §3 CONTRACT; the orchestrator restored it from state.json's `freeze`/`tripwire` metadata (version v1, contract_md5, approved_by) rather than from memory — verify then independently re-confirmed the restored text against the test file's own docstring contract-restatement and the live implementation, and `add.py check`'s tamper-tripwire reported clean. Lesson: `advance --fill` should refuse (or at minimum loudly warn) when targeting a section the engine's own freeze metadata marks FROZEN, rather than relying on a downstream restore-from-metadata recovery (evidence: this incident; no test currently pins `--fill` against a frozen §3 — a coverage gap worth a future task, not fixed here).

