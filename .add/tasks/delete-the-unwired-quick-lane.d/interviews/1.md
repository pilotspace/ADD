---
type: Interview
task: /tasks/delete-the-unwired-quick-lane.md
by: Tin Dang
at: 2026-09-03
interview: sha256:07959cf1781fbaea
---
# interview 1 — delete-the-unwired-quick-lane

## A1 [who]
- asked: the ONE freeze approval, since `quick` froze at `process` with no authority argument
- if wrong: a lane that closes at `process` is the seam's whole point defeated
- answered: confirm

## A2 [which]
- asked: every PUBLIC function in add.py, since a private helper is reached only through one
- if wrong: a private bypass would go unseen
- answered: confirm

## A3 [when]
- asked: DELETE for `quick` — it was reachable from no verb and named in no doc, so nothing depended on it
- if wrong: deleting a used feature would break callers
- answered: confirm

## A4 [absent]
- asked: DELETE, per the human — `checks_sync` was the dead WRITER, while `checks_verify` is live (`doctor` calls it for `checks_citation`) and stays
- if wrong: deleting both would have removed a shipped check
- answered: confirm

## A5 [order]
- asked: the guard FIRST so it is observed red
- if wrong: a guard written after the fact proves only that the code compiles
- answered: confirm

## A6 [experience]
- asked: the engineer adding a convenience verb months from now
- if wrong: a bare "assertion failed" teaches nothing
- answered: confirm

## R:BYPASS [reject]
- asked: a single call carries a node from creation to closed with no human seam
- answered: confirm
