---
type: Task
title: The skill stops promising a fallback the engine removed
status: done
depth: standard
milestone: adoption-beyond-code
scope:
  - add-method/skill/add/
  - add-method/src/add_method/_bundled/skill/add/
  - .claude/skills/add/
  - add-method/tests/skill/
gives:
  - S1 the `SKILL.md` orientation branch that tells an agent how to create a bundle
  - S2 the `domains.md` section that tells an agent how to frame a non-code bundle
generated: { by: add/3.1.0, at: 2026-08-12 }
verified:
  - { by: "Tin Dang", at: 2026-08-12, act: freeze, authority: human, direction: "sha256:82095d73dfbc146f" }
  - { by: "cli", at: 2026-08-12, act: brief, authority: process, brief: "sha256:bdf01c1bed76a756" }
  - { by: "process:run", at: 2026-08-12, act: run, authority: process, outcome: PASS, receipt: /tasks/skill-profile-truth.d/runs/1.md }
  - { by: "Tin Dang", at: 2026-08-12, act: gate, authority: process, outcome: PASS, receipt: /tasks/skill-profile-truth.d/runs/1.md, brief: "sha256:bdf01c1bed76a756" }
advised_by: method-steward
---
## CARD
goal: no shipped skill surface still describes the silent `code` fallback that `profile-refusal` deleted
why: `profile-refusal` changed the engine's behaviour, and two skill files now describe the old one. `SKILL.md` tells an agent `init` "writes the `code` lenses under any other name without refusing"; `domains.md` tells it `init` "accepts any string and then silently writes the `code` lenses under that name". Both were true this morning and are false now — and both ship in all three skill trees, so six files carry the same dead warning. This is the milestone's own defect class turning up inside the milestone: an engine change invalidated prose, and nothing failed. The cost is not cosmetic. `domains.md` uses that claim as the ARGUMENT for a rule ("do not invent a profile — because it fails silently"). The rule survives; its reason does not, and a reader who tests the stated reason and finds it false has no cause to keep the rule.
beat: done · next: add status

## RULES
<must>
- M1 no shipped skill file claims `init` accepts, tolerates, or silently falls back on a profile the engine does not ship
- M2 every profile the skill names is one the engine actually ships, and every shipped profile is named
- M3 the three skill trees stay byte-identical, and `SKILL.md` stays within its line pin and the total surface budget
- M4 the guard derives what it knows about profiles from the engine, so the day a third profile lands the skill is checked against it rather than against a list written today
</must>
<reject>
- R:RULEWITHOUTREASON a rule may not be left standing on a reason the engine no longer supports — restate the reason or drop the rule -> "rule_without_reason"
- R:PINNED the guard must not compare against a literal copy of today's `PROFILES` -> "pinned"
</reject>

## ASSUMPTIONS
- A1 [who] covers: S1, S2 · the request does not say whether the two agent-facing context files the installer drops (`CLAUDE.md` / `AGENTS.md` guideline blocks) count as shipped skill surface; taking it as the `skill/add/` trees only, because those blocks are generated by `sync-guidelines` from a source this task does not touch -> if wrong, an agent reading the dropped block keeps the dead claim and the fix is invisible where most agents actually read
- A2 [which] covers: S1, S2 · it does not say whether prose describing the refusal itself may quote the old behaviour to explain what changed; taking it as allowed only when the sentence marks it as former, since explaining a change is not the same as promising it -> if wrong, the guard forbids any changelog-shaped sentence and the skill cannot say what moved
- A3 [when] covers: S1, S2 · it does not say whether the check runs against the canonical tree or all three; taking it as all three, because a claim is shipped the moment any tree carries it and the `_bundled` copy is what pip installs -> if wrong, a tree drifts and the guard reports agreement it never verified
- A4 [absent] covers: S1, S2 · it does not say what a skill file that mentions no profile at all should do; taking silence as compliant — the rule forbids a false claim, it does not compel every file to discuss profiles -> if wrong, files are padded with profile prose to satisfy a checker, which spends the surface budget on nothing
- A5 [order] n/a · the profile set is a mapping and every claim about it is a membership statement; no check depends on the order the skill lists them

## PLAN
contract: `SKILL.md` and `domains.md` describe the refusal instead of the fallback, in all three trees, and `domains.md`'s "do not invent a profile" rule is restated on a reason that is still true — the engine refuses, and a name it does not ship buys nothing. A new guard reads `add.PROFILES` and fails on any shipped skill file that names a profile outside it, claims a silent fallback, or leaves a shipped profile unnamed.
scope: add-method/skill/add/, add-method/src/add_method/_bundled/skill/add/, .claude/skills/add/, add-method/tests/skill/

## EDGES
- E1 the budget is the constraint that bites: `SKILL.md` is pinned at 176 lines and the total skill surface at 1500. The replacement sentences must fit without spending the surface a later task needs, so the fix is a rewrite in place, never an addition.

## CHECKS
- test_skill_claims_no_silent_profile_fallback · covers: M1, R:RULEWITHOUTREASON · no shipped skill file, in any tree, says `init` accepts/tolerates/silently-writes an unshipped profile except in a sentence that marks the behaviour as former
- test_skill_names_only_shipped_profiles · covers: M2, M4, R:PINNED · both directions against `add.PROFILES`, with a fabricated mapping proving the guard reads the engine rather than a literal
- test_all_three_skill_trees_agree · covers: M3, A3 · the three trees digest identically for both touched files
- test_skill_surface_within_budget_after_edit · covers: M3, E1 · `SKILL.md` within its line pin and the total surface within 1500 after the rewrite
red-first: 1 of 4 is red at freeze, on all 9 instances (2 files × 3 trees, plus the two-line claim in `domains.md`). The other 3 are GREEN by design — they guard what the rewrite must not break: the trees already agree, both shipped profiles are already named, and the surface is already inside budget. One check defect was found and fixed before freeze: the budget check re-derived its own measurement with a plain `rglob` and reported 2068 against a 1500 pin that `test_surface.py` passes comfortably, because it counted the nested `persona-author/` sub-skill that carries its own budget. A second opinion about a pinned number is a contradiction, not a check — it now calls `test_surface._own_docs()` directly.

## EVIDENCE
receipt: <runs/<n>.md>
gate: <PASS | RISK-ACCEPTED | HARD-STOP>

## LESSONS
- <lesson> -> add learn <lens>
