# dependencies.allowlist — one package per line; the corpus (which CI runs) rejects
# any declared RUNTIME dependency not listed here (test_dependency_allowlist.py).
# Defeats the AI "invented a plausible package name" supply-chain risk.
#
# Python tooling is standard-library only (pyproject dependencies = []).
# Node installer carries exactly one approved runtime dependency:
@clack/prompts
