# Method — the ADD spec

project: AIDD / ADD Methodology · seeded: 2026-07-17 · stage: mvp

> Living document — how we work: the loop, autonomy, ceremony budget (ADD).
> Keep the sections below CURRENT (state, not history); lessons land under
> Deltas the moment they are learned: `add.py delta-append add "<lesson>"`.
> A delta that changes the standing picture is folded UP into the sections
> above it and marked `[folded]` — the Deltas list is the inbox, not the spec.

## Now
<!-- migrated from PROJECT.md §Domain/§Spec loop-rules @ fv66 (foundation-split) -->
- The loop: INTAKE sizes a request into a milestone; each task runs the specification
  bundle (Spec+Scenarios+Contract+Tests) to ONE human approval at the frozen contract,
  then a self-driving build→verify run. Lifecycle above the task:
  `milestone-done → fold → compact → archive → (repeat ≥1×) → release → watch` —
  the engine RECORDS, the human SHIPS (never tags/publishes/deploys). [fv34]

## Decisions that bind
- Verify re-checks the milestone's frozen exit criteria after a task changes shape — a shared-contract drift turns a gate red, never slips silently to milestone close. [v9 · SDD]
- Residue an evidence auto-gate must escalate is not limited to security·concurrency·architecture — method/trust-layer edits are a residue category. [v6]
- Additive content folds into a frozen contract with NO re-freeze: removes no frozen §3 section, changes no reject → it is INSIDE the frozen shape; likewise a presentation/layout layer iterates freely without a re-freeze — the freeze binds the data/interface seam. [fv33 · udd-design-loop]
- A frozen descriptive annotation can be wrong while the binding seam holds: honor the seam, disclose at verify, never silently retrofit the frozen text. [fv33]
- A defect found while working task N in an already-done task M is fixed by the recorded `reopen M` (gate reset), never a silent out-of-scope edit. [fv21 · foundations-chapter]
- Foundation specs are append-only NEWEST-FIRST and compact by the per-spec rolled settled line (summarize + `see git`, NEVER delete; open residue stays live) — the ritual is convention-guided (`compact-foundation.md`), deliberately no engine verb. [fv31 · foundation-compaction]
- The skill lean fence is a hard floor: genuinely-new doc-truth on a guide is reclaimed from the same guide's prose, not a budget rebaseline, absent an explicit human bump. [fv58 · component-worked-example]

## Deltas (newest first)
- [open · 2026-07-26] A frozen contract can encode a false assumption about the surrounding system, and the build is where you find out: mine said the interrupt watcher polls a streaming transcript, but the runner drained stdout with communicate() and wrote the transcript only after exit. The honest move is to make the assumption TRUE where that is defensible (stream on the new path only, so the old path stays identical by construction) rather than silently redesign around a frozen clause. (task:bench-interrupt-mechanics)
- [open · 2026-07-26] Before inventing a mechanism for a defect, grep for how the repo already cured that class: the benchmark had fixed editable-install leakage in running_app with -E -s -S months earlier, and the CLI probe was simply the one spawn path that never got the flags. I shipped a NEW provenance mechanism for an already-solved class. The cheaper and more consistent fix is to find the existing cure and apply it where it was missed — and a drift test between the two call sites is what keeps them from separating again. (task:bench-ambiguity-scoring)
- [open · 2026-07-25] Never reach for a hard reset to move a commit off the wrong branch: it discards the uncommitted working tree too. Branching then hard-resetting main destroyed four in-flight source edits AND the engine state file, while untracked files survived — so the damage was ragged and easy to under-read. The safe move is branch, then reset --keep or reset --soft; and when the tree is dirty, verify what is uncommitted BEFORE any reset. (task:risk-proportional-skip)
- [open · 2026-07-25] Workspace COPY isolation cannot defend against a pip editable install: a benchmark run left an editable install of its own workspace in the machine's global site-packages, so every later run resolved that arm's app instead of its own. An editable finder sits on sys.path for every process on the machine — there is nothing to copy away from. Hermeticity has to be proven at the probe (does this entry point live under the workspace under test?), not assumed from the sandbox. (task:risk-proportional-skip)
- [open · 2026-07-25] test_template_flag_vocabulary assumes every `--flag` appearing under templates/ is an add.py flag — true while templates held only ADD documents, now FALSE because seeded personas cite real tool commands (`git log --oneline`, `git diff --stat` both tripped it). Worked around in prose this time rather than relaxing a guard to pass my own build, but the assumption will fire again on the next seeded persona that names a real command; the guard needs an add.py-flag-context scope (evidence: seed-method-personas, 2 findings on the first two seeds authored) (task:seed-method-personas)
- [open · 2026-07-25] re-cross re-snapshots the tamper tripwire AT THE MOMENT IT RUNS, so it must be the LAST act before verify, not the first act of the fix: re-crossing and then editing tests re-arms the tripwire against the very edits that were approved, and the gate returns to build with tamper_detected naming all of them. Sequence is: agree the change, make every test edit, THEN re-cross, then gate (evidence: seed-method-personas gate PASS refused with build_tampered on 5 files after an approved re-cross preceded the edits) (task:seed-method-personas)
- [open · 2026-07-25] new-task silently inherits the ACTIVE milestone even when that milestone is CLOSED-but-unarchived, reopening it: creating seed-method-personas flipped persona-template-completeness from DONE 4/4 to ACTIVE 4/5 and would have made its signed close a lie. Recovery is set-milestone <task> none. Either new-task should refuse to attach to a done milestone (or warn), or a closed milestone should stop being the inheritance default (evidence: report before/after; state.json milestone.status was already "done" while the report read ACTIVE purely from the unfinished member task) (task:seed-method-personas)
- [open · 2026-07-25] verification EVIDENCE must be dated to the commit it covers: PR #181 quoted "full suite 2316 OK" from the milestone commits, but the later SKILL.md commit (60381b73) was verified with only 34 targeted tests + add.py check — neither runs wording_lint — so an enforced banned idiom ("the seam", WORDING_RUBRIC.md:69) shipped and turned both CI Tooling-tests jobs red. A prose edit to a LINTED surface needs its linter, and a suite result never carries forward to a commit made after it ran (evidence: d77054ea; test_autonomy_command.WordingFenceTest + test_ci_tooling_mirror_gap fresh-checkout, one root cause seen twice) (task:lock-reclaim-hardening)
- [open · 2026-07-25] a queued todo can go stale silently when the artifact it names gets RENUMBERED: todo #38 asked to relabel "§5 Scope" as HARD, but the section fold moved build-strategy to §3 and d96010a4 (2026-07-23) already delivered the relabel — the todo survived because its section pointer no longer matched anything to grep. Before opening a queued task, git log -S the exact phrase it asks for against the TRACKED twin (the root .add/tooling/ tree is gitignored, so git log there returns empty and reads as "never done") (task:lock-reclaim-hardening)
- [open · 2026-07-25] backticks inside a DOUBLE-quoted shell string are command substitution in zsh — a delta-append carrying `verb` markup silently loses those words (they run as commands and expand to nothing). Single-quote any delta text containing backticks, or drop the markup (evidence: a persona-roster delta landed as "printed by and ," with both verb names eaten; zsh reported "command not found: status") (task:preset-patterns-fold)
- [open · 2026-07-25] the persona ROSTER line (slug[flow] - vibe) is printed by 'status --all' and 'check', NOT by plain 'status' (which prints only a count). A skill instruction naming the wrong verb costs the reader a wasted call and then a whole-roster body read — the thing the roster line exists to avoid. Verify the verb prints what you claim before citing it (evidence: plain status shows 'personas: 9 (status --all)'; the roster block is under status --all) (task:preset-patterns-fold)
- [open · 2026-07-25] SKILL.md is the ONLY file the orchestrator reads (agents/add-worker.md says so), so any rule that lives solely in an agent file is invisible to the orchestrating loop. The 'no persona fits -> use the generic fallback, never block' rule was agent-only; the loop could stall on a missing persona. Check which surface actually READS a rule before deciding where to write it (evidence: agents/add-worker.md 'the orchestrator reads only SKILL.md and does not pre-read it for you'; SKILL.md:72 persona-seeding ladder added inline) (task:preset-patterns-fold)
- [open · 2026-07-25] add-method/build/ is gitignored but NOT pruned from the engine scope walk, so touching the setuptools cache trips a pending scope_violation. Either declare build/ in §5 Scope when a task rebuilds a package, or clean the cache before the scope snapshot (evidence: preset-patterns-fold check WARN naming add-method/build/lib/add_method/__init__.py after a pip wheel run; contrast the *.egg-info suffix-prune already in _scope_walk) (task:preset-patterns-fold)
- [open · 2026-07-25] a green suite does not prove a clean ARTIFACT: the full 2316-test suite passed while the wheel still shipped _template.md.tmpl (retired at e29ddac4) out of the stale gitignored add-method/build/lib/ setuptools cache. Tests read the source trees; only building and inspecting the wheel + tarball reads what users receive. Build and unzip -l both artifacts before claiming a retirement shipped (evidence: preset-patterns-fold refute-read (4); wheel entry add_method/_bundled/tooling/templates/personas/_template.md.tmpl dated 06-30-2026) (task:preset-patterns-fold)
- [open · 2026-07-25] a shipped artifact with no consumer is a lie about how the method works: 12 preset persona templates survived e29ddac4 (which retired the static template for the persona-author skill) and shipped in every npm tarball and pip wheel for months, looking authoritative while nothing loaded them. When retiring a mechanism, grep for the ORPHANS it leaves behind, not just the entry point (evidence: SETUP_FILES has no persona entry; cmd_init creates .add/personas/ empty; git log on templates/personas/ ends at e29ddac4) (task:preset-patterns-fold)
- [open · 2026-07-25] the tooling TEMPLATES tree is FOUR-way, not the three-way engine trio: add-method/tooling · .add/tooling · _bundled/tooling · AND add-method/.add/tooling (the dogfood install inside add-method/). A three-tree edit leaves a partial deletion that only test_template_flag_vocabulary.TemplateTreeParity catches — enumerate the trees with a find before declaring §5 scope for any templates change (evidence: preset-patterns-fold first full-suite run, 2316 tests FAILED(1) on a file-set mismatch) (task:preset-patterns-fold)
- [open · 2026-07-25] a backtick span that wraps across a newline leaves everything after it unprotected: release-planner.md tripped the bare-<...>-placeholder quality WARN because 'git rev-parse <tag>' sat after an unbalanced backtick on the wrapped line. Keep any code span on ONE physical line in a persona or template (evidence: _persona_quality_warnings strips inline spans with a no-newline regex; add.py check WARN on release-planner) (task:planner-personas-seed)
- [open · 2026-07-25] a persona-roster boundary is only ASSERTED at authoring time — a use-when disjointness check proves no shared trigger phrase, not that two lenses would never both be plausible for one request. The real proof is route-outcome data per task-kind over several milestones. Re-check the three planners against method-product-owner once the scoreboard has traces (evidence: .add/tasks/planner-personas-seed/PLAN.md refute-read residual) (task:planner-personas-seed)
- [open · 2026-07-25] a forward-pointing parenthetical ('X still lags; the agent is accurate until that chapter catches up') is a time bomb in a reference doc — it went stale the moment the very next task fixed the chapter. Prefer stating what is true; if a gap must be named, make closing it a Must of the task that closes it (evidence: contract.md Abilities entry, added by persona-template-legs and removed by persona-docs-truth one task later) (task:persona-docs-truth)
- [open · 2026-07-25] point a doc-truth check at the task's OWN output, not only at the defect it was written to catch: check_no_dead_path found a dead link this task INTRODUCED (agents/add-worker.md, which exists only under .claude/ and add-method/), not the retired _template.md.tmpl it was designed for (evidence: .add/tasks/persona-docs-truth/PLAN.md §5 Strategy actually used) (task:persona-docs-truth)
- [open · 2026-07-25] a BARE filename in a §5 Scope declaration resolves as a sibling of the PREVIOUS token's dir, so declaring two same-named mirrors (add-method/docs/18-personas.md + the repo-root twin) silently collapses to one path and leaves the mirror uncovered. Read the freeze echo's scope lines and count them against the mirror set before building; declare any root-level file with a '/'-bearing token (evidence: add.py:5165 _declared_scope; the v1 freeze of persona-docs-truth echoed 5 paths for a 6-token declaration) (task:persona-docs-truth)
- [open · 2026-07-25] an engine docstring can be stale without being wrong-in-behavior and still cost a repin to fix: io_state.py:273 still describes a '_template.md scaffold' for a persona template retired long ago. Editing it re-aims ENGINE_PKG_MD5 for zero behavior change, so it is deliberately deferred rather than folded into a prose task (evidence: engine_manifest.package_files walks add_engine/*.py; .add/milestones/persona-template-completeness/MILESTONE.md Scope Out) (task:persona-template-legs)
- [open · 2026-07-25] read-before-you-assert applies to RESEARCH, not just deliverables: the 'Abilities is loaded by no surface' finding was drawn from three surface docs (design.md, phases/verify.md, 18-personas.md) without opening agents/add-worker.md, which already instructs the roster agent to read the whole persona body and run its lead commands. A survey that enumerates consumers must open the AGENT files, not only the guide files (evidence: .add/tasks/persona-template-legs/PLAN.md refute-read; agents/add-worker.md:38,43) (task:persona-template-legs)
- [open · 2026-07-23] changing _scope_walk's own prune set mid-task makes the freeze-time snapshot disagree with the gate walk (pruned files read as touches) — re-cross after the walker edit is the one-time migration (evidence: scope-walk-prune's own gate) (task:scope-walk-prune)
- [open · 2026-07-23] a global str.replace fill script rewrote the frozen §3 (its target string appeared INSIDE the contract block) and tripped contract_tampered — anchor §5/§6 fills to their section or use line-scoped edits; repair = revert + re-cross --by <human> (evidence: scope-first-freeze return_to_build attempt 1) (task:scope-first-freeze)
- [open · 2026-07-23] A milestone-level ## Strategy slot mirrors a task's Build-strategy: SOFT/advisory, drafted-blank valid, placed AFTER ## Exit criteria so it stays OUT of the ## Tasks DAG-parse span (_compile_task_graph bounds ## Tasks at the next ## heading). Renders verbatim via new-milestone — zero engine parse, zero ENGINE_MD5 churn (evidence: strategy-section PASS, live confirm/check clean). (task:strategy-section)
- [open · 2026-07-23] _FALLBACK_TASK + skill guides needed NO change — they never carried the SOFT-mislabel or a required-Persona (the terse _FALLBACK has no qualifier + no Persona line; build.md already frames the Persona overlay as optional). Verifying 'already consistent' is an honest close, not a skipped scope (evidence: grep of guides + _FALLBACK for SOFT/required = clean). (task:trim-build-strategy-labels)
- [open · 2026-07-23] A 'trim' milestone can be a pure RELABEL — the enforced Scope (may touch) line stays HARD scope-lock; only the human-facing framing + Persona/Regression optionality changed. No engine kernel removed, no gate weakened (evidence: trim-build-strategy-labels PASS, full suite 2253/0). (task:trim-build-strategy-labels)
- [open · 2026-07-23] a milestone task's premise can go OBSOLETE under sibling work finishing first — grounding engine-kernel-trim proved its 'usage-mined strike-list' was EMPTY (all 34 verbs load-bearing: 6-57 test invokes each; 'guide-unreferenced' is a false dead-code signal) and its dedup was already delivered (14 add_engine modules + engine-hygiene). The honest close is retire-as-superseded (no freeze on empty scope; signal-graph precedent), and produce a dependent task's evidence artifact DIRECTLY (CENSUS.md) rather than run the redundant standalone task. Also: an exit criterion can be MIS-TOOLED — call-census's _engine_call_census counts ALL add.py calls (median 9.3/task incl. orientation) while the criterion means the ≤3 CROSSING calls (median 3.5, within the milestone's own 'happy path not a cap' clause). evidence: thin-engine-loop closed 6/6, 105 archived (task:gate-experience-udd)
- [open · 2026-07-23] a long-stale frozen contract (frozen 2026-07-16, tree moved for months) carries literal anchors that later unrelated work supersedes — test names removed by corpus-slim, byte ceilings grown by other tasks, ENGINE_MD5 rotated. The honest close is NOT to edit the frozen §3 but to (a) pin DRIFT-STABLE invariants in the acceptance test, (b) re-resolve every stale anchor in §6 Live-verify rather than silently, (c) fix the build_tampered + missing scope-snapshot via signed re-cross --by <human> (the sanctioned post-freeze-test-addition path), then gate. evidence: gate-experience-udd PASS after re-cross; suite 2247/0 (task:gate-experience-udd)
- [open · 2026-07-23] Repo-root book-mirror files can't be named by the bare scope grammar — use the add-method/../<name> climb (slash-bearing → resolves at project root) on ONE physical Scope line; a mkdocs config change required by a docs Must (M6 mermaid-enable) is IN-scope and belongs on that same line (evidence: 17-token re-cross resolved clean, gate PASS no scope_violation) (task:book-de-scenarios)
- [open · 2026-07-23] Book de-scenarios fold: retire-in-place mirrors the engine's non-contiguous §1·3·4·5·6·7 in prose too — the frozen §1 Boundary (structural = numbered 'Step 2'/'§2'/file-link; bare 'Scenarios' = activity, may stay) is what let a mechanical grep-check cleanly separate must-change from may-keep (evidence: 12-roles RACI merged Scenarios→Tests&Scenarios column, but skip-cost/profile-rigor 'Scenarios' rows kept) (task:book-de-scenarios)
- [open · 2026-07-22] prefer minimal inline distillation over a parallel reference doc: a separate reasoning.md + pointer + test was apparatus the human vetoed — the umbrella principle belongs INLINE where its checks already live (direction.md), guarded by extending the existing test, not a new file (evidence: task fable-thinking-reference redirected from doc to one Fluent≠true clause) (task:fable-thinking-reference)
- [open · 2026-07-22] fable Floor pass: the fable-thinking protocol's front-half (claim grammar OBSERVED/DERIVED/PRIOR/ASSUMED · Goal+Leftovers Floor · GROUND observation-over-memory · output-shape constraint-loop) fills ADD's empirically-thinnest reasoning spots — measured 9/27/32 hits per 3021 fable thinking blocks vs 334 for mechanism; landed in add-advisor.md + direction.md, zero SKILL.md growth (evidence: task fable-floor-reasoning, test_fable_floor 4/4) (task:fable-floor-reasoning)
- [open · 2026-07-22] rolling a ship bullet that carries an explicitly-OPEN tail needs a supersession note on its ledger line (settled-since | superseded-by | carried-unverified) — else open work hides behind the see-git pointer (evidence: foundation-split F3, ledger #26/#30/#32/#34 annotated post-refute) (task:foundation-split)
- [open · 2026-07-22] a two-clause Must needs one acceptance check per clause — foundation-split A5 proved M5's ceremony-collapse half but never its ship-rows-stay clause; only the cross-agent refute-read's fresh ledger sampling caught the silent deviation (evidence: F1, 15 ship rows restored byte-identical from git) (task:foundation-split)
- [open · 2026-07-17] lessons now land in-flight via delta-append; the milestone-close fold consumes .add/specs/, not a batch recall
<!-- prepended by `add.py delta-append add "<text>"` — one line per lesson, `- [open · <date>] <lesson>` + the active-task stamp; fold a delta upward, then retag [open]->[folded] -->
