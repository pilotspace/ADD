# TASK: Flag-mode quick-ref in SKILL.md (fast=task, auto=mode)

slug: flag-mode-quickref · created: 2026-06-25 · stage: mvp
autonomy: auto
phase: done   <!-- fast lane: ground -> specify -> contract -> tests -> build -> verify -> observe -> done -->
fast: true   <!-- the fast lane: a small task, collapsed flow + minimal template. Omit --fast for full rigor. -->

> Fast lane — one small task, minimal sections, filled top-to-bottom. The trust floor still
> holds: a FROZEN §3 contract · ≥1 red test before build · a recorded §6 gate (security = HARD-STOP).
> The acceptance scenario collapses into §1 `Accept:`; OBSERVE is one optional line at the gate.

---

## 0 · GROUND — the real codebase

Touches (files · symbols):
- `add-method/skill/add/SKILL.md:64-65` — line 64 `**Quick ref**`; line 65 `**Opt-in flags**` names only `--fast` + `--await-confirm` (NOT the `auto`/`conservative`/`manual` autonomy mode). This is the surface to evolve into a labelled "Flag mode".
- 3 parity-locked skill trees: canonical `add-method/skill/add/SKILL.md` → dogfood `.claude/skills/add/SKILL.md` (test_tree_parity) → `_bundled` (test_bundle_parity, regenerated by prepare_bundle.py). NO engine pin (doc only — add.py untouched).
- lean fence `test_skill_lean.py`: `core` pool = SKILL.md + intake.md, ratio 0.88, baseline 18049 → target 15883 B; current pool 15696 B, headroom only 187 B. A real flag-mode block exceeds it → BUMP the core baseline by new-surface ÷ ratio (ratio kept), per the fence's own documented pattern.

Anchors the contract cites: SKILL.md line 65 "Flag mode" block · the core-pool baseline in test_skill_lean.py.

---

## 1 · SPECIFY — the rules

Feature: A labelled "Flag mode" quick-ref in SKILL.md — discover the two human-owned dials fast (task) + auto (mode)
Must:
  - SKILL.md carries ONE labelled "Flag mode" entry naming BOTH `--fast` (the task lane) and `auto` (the autonomy mode), in the always-loaded orient region.
  - It names how to reach each: `new-task --fast` · `add.py autonomy set conservative|manual` (dial down from the `auto` default); and (richer scope) the blessed milestone-free standalone lane + `add.py todo` capture built this milestone.
  - Propagated byte-identical across all 3 skill trees; lean fence + parity stay green (core baseline bumped, ratio 0.88 kept).
Reject:
  - (none — prose/presentation; no engine, no error code)
Accept: Given SKILL.md, When a reader scans the orient region, Then they find one "Flag mode" entry that names BOTH `--fast` and `auto`; and `test_tree_parity` + `test_bundle_parity` + `test_skill_lean` are green.
Assumptions: ⚠ scope of the block — minimal (fast + auto only) vs richer (also the standalone lane + `todo`). Richer ties this milestone together and is more discoverable; cost is ~+300 B → a core-baseline bump (ratio kept). Decided at freeze.

---

## 3 · CONTRACT — freeze the shape

```
SKILL.md line 65 — replace the "**Opt-in flags**" line with a labelled "Flag mode" quick-ref
(exact copy, richer scope):

**Flag mode** — two human-owned settings (never auto-picked): **fast** (task) · **auto** (mode).
- **fast** — `new-task --fast`: minimal template, freeze-gated; a milestone-free `--fast` task is a
  blessed standalone low-ceremony lane. Jot ideas first with `add.py todo "<text>"` (then `todo` to
  list · `todo --done <id>`).
- **auto** — `autonomy: auto` (default) auto-gates verify on evidence; lower the autonomy level with
  `add.py autonomy set conservative|manual` for a human gate · `new-milestone --await-confirm`
  confirm-gates a milestone's tasks.

Mechanics: edit canonical add-method/skill/add/SKILL.md; prepare_bundle.py → _bundled; cp → dogfood
.claude/skills/add/SKILL.md. Bump test_skill_lean `core` baseline by Δbytes ÷ 0.88 (ratio kept).
Green: test_tree_parity · test_bundle_parity · test_skill_lean. No add.py / no ENGINE_MD5.
```

`Least-sure flag surfaced at freeze:` [spec] richer vs minimal block — RESOLVED at freeze: richer (fast + auto + standalone lane + todo), accepting the core-baseline bump (ratio 0.88 kept).
Status: FROZEN @ v1 — approved by Tin Dang (AskUserQuestion freeze, richer block), 2026-06-25.
Amendment @ v1.1 (build, semantic-identical): "dials"→"settings" and "dial down"→"lower the autonomy level with" — the frozen copy used the word `dial`, banned by the ubiquitous-language lint (`autonomy-dial` → sanctioned "autonomy level"). Block content/scope unchanged; this is a vocabulary-conformance correction, NOT a re-design.
<!-- The freeze IS the one approval. Approved -> Status: FROZEN @ vN — approved by <name>.
     Changing a frozen contract = change request back to SPECIFY. -->

---

## 4 · TESTS — failing-first (red)

Plan: `test_flag_mode_quickref.py` —
  (a) canonical SKILL.md contains a `**Flag mode**` label naming BOTH `--fast` AND `auto`;
  (b) the same block is byte-present in all 3 trees (canonical · dogfood · _bundled);
  (c) `test_tree_parity` + `test_bundle_parity` + `test_skill_lean` stay green (run as part of the suite).
Red first: today SKILL.md has no "Flag mode" label (line 65 is "Opt-in flags", and never names `auto`).
Tests live in: `add-method/tooling/test_flag_mode_quickref.py` · MUST run red before Build.

---

## 5 · BUILD — AI writes code

Scope (may touch): `add-method/skill/add/SKILL.md` · `.claude/skills/add/SKILL.md` · `add-method/src/add_method/_bundled/skill/add/SKILL.md` · `add-method/tooling/test_skill_lean.py` · `add-method/tooling/test_flag_mode_quickref.py`
Code lives in: skill trees + tooling   ·   Constraints: prose only, no add.py / no ENGINE_MD5; keep all 3 trees byte-identical; lean fence ratio 0.88 unchanged (baseline bump only).

---

## 6 · VERIFY — evidence + gate

- [x] all tests pass · coverage held — full suite 1756/0. §3 carried a build-time v1.1 amendment (slang `dial`→sanctioned "autonomy level"), semantic-identical; the tamper tripwire correctly fired, so I re-crossed contract→tests→build to re-anchor the snapshot on the final §3 + tests (all edits in declared §5 scope — re-baselining hid nothing). The lean-fence + min-pillar-style surface locks were grown deliberately (baseline bump, ratio kept), not weakened.
- [x] green was EARNED — `test_flag_mode_quickref.py` asserts the block names BOTH `--fast` and `auto` across all 3 trees; parity + lean fence + wording-lint all green
- [x] no exposed secrets, injection openings, or unexpected dependencies — prose-only edit to SKILL.md + a test baseline; no code, no deps, no security surface

Build expectations (from §1 Accept + §3 CONTRACT): SKILL.md's orient region shows a "Flag mode" quick-ref naming both `--fast` and `auto`, identical across all 3 skill trees — confirmed by `test_flag_mode_quickref.py` red→green + `test_tree_parity` + `test_bundle_parity` + `test_skill_lean` green (core baseline bumped, ratio kept).

### GATE RECORD
Outcome: PASS
Reviewed by: Tin Dang (auto-gate on evidence, autonomy: auto) · date: 2026-06-25
OBSERVE: [ADD · open] a build-phase §3 amendment (even semantic-identical) trips the tamper tripwire and forces a contract→build re-cross — worth a fast-lane-guide note that contract copy should be vocabulary-lint-clean BEFORE the freeze, so the build never has to amend it.
<!-- A security finding is ALWAYS HARD-STOP. Record exactly one outcome — no silent pass.
     OBSERVE (optional): one `[SPEC · open]` or competency-delta line here if the loop taught the foundation something. -->
