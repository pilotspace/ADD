[pytest]
markers =
    regression: WM3 oracle re-exports of earlier-WM oracle tests (regression_rate signal)
    slow: touches the real filesystem/uv (bench-pilot-report M6) — allowed to require `uv` on PATH
